using UnityEngine;

public class Floor : MonoBehaviour
{

    public float speed = 1.00f; //地板移動速度
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
